{{#partial "content"}}
    <div class="container" id="data">
        <div class="container">
                {{#if rangers}}
                    {{#each rangers}}
                        <p>{{rangerName}}</p>
                        <a href="/rangers/{{id}}" class="btn btn-success">Information</a>
                    {{/each}}
                {{/if}}
        </div>
    </div>
{{/partial}}
{{> layout.hbs}}