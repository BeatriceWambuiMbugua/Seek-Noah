{{#partial "content"}}
    <div class="container" id="data">
        <div class="container">

                <div class="col-md-6">
                    {{#if locations}}
                        {{#each locations}}
                            <p>{{locationName}}</p>
                            <a href="/locations/{{id}}" class="btn btn-success">Information</a>
                        {{/each}}
                    {{/if}}
                </div>
        </div>
    </div>
{{/partial}}
{{> layout.hbs}}